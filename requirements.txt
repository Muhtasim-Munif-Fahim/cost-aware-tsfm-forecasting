# Core harness
numpy
pandas
lightgbm
matplotlib
scikit-learn        # Ridge/StandardScaler for chronos_cov
shap                # specialist interpretability (single mode --shap)

# Foundation-model tier (optional but required for --with-chronos; GPU recommended)
torch
chronos-forecasting

# Second foundation-model family (required for --with-timesfm; the R1.1 generality check).
# Install with the torch extra: pip install "timesfm[torch]"
timesfm

# Energy measurement (required for --measure-energy). The version matters: it governs the
# CPU power model behind every reported joule, so it is pinned and recorded in each run's
# _runconfig.json.
codecarbon==3.2.8
